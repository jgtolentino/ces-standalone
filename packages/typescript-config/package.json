{
  "name": "@ai/typescript-config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript configuration for AI Agency monorepo",
  "files": [
    "*.json"
  ],
  "scripts": {
    "build": "echo 'TypeScript config built'",
    "test": "echo 'TypeScript config tested'"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}