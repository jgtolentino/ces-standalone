{
  "version": 2,
  "buildCommand": "pnpm install && pnpm build",
  "outputDirectory": "tenants/ces/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_VERCEL_ENV": "production"
  },
  "builds": [
    {
      "src": "tenants/ces/package.json",
      "use": "@vercel/next"
    }
  ]
} 