{
  "buildCommand": "cd ../.. && pnpm --filter=\"@ai/db\" --filter=\"@ai/ui\" --filter=\"@ai/agents\" --filter=\"@ai/chat-ui\" --filter=\"@ai/dashboard-ui\" build && cd tenants/ces && npm run build",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "NODE_ENV": "production",
    "SKIP_ENV_VALIDATION": "true"
  },
  "regions": ["iad1"],
  "outputDirectory": ".next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./",
  "crons": []
}